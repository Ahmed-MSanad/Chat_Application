import { FilterUsersPipePipe } from './filter-users-pipe.pipe';

describe('FilterUsersPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUsersPipePipe();
    expect(pipe).toBeTruthy();
  });
});
